# pretty print integers
```bash
printf "%'d\n" "$(find $CLONES -type f -exec openssl dgst -sha256 {} + | wc -c)"
```
# Floating point (non-POSIX but widely supported)
```bash
printf "%'.2f\n" 1234567.89
```
